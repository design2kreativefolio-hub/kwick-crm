# ---- Core ----
Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-filter==24.2

# ---- ASGI / real-time ----
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
uvicorn[standard]==0.30.1

# ---- Async tasks ----
celery==5.4.0
django-celery-beat==2.6.0
redis==5.0.7

# ---- Database ----
psycopg[binary]==3.1.19

# ---- Storage (OVH Object Storage, S3-compatible) ----
django-storages==1.14.3
boto3==1.34.131

# ---- Web push ----
pywebpush==2.0.0
py-vapid==1.9.0

# ---- PDF generation (Employee Collaterals / invoices) ----
weasyprint==62.1

# ---- Image handling (avatar upload validation) ----
Pillow==10.4.0

# ---- Config / server ----
python-dotenv==1.0.1
gunicorn==22.0.0
whitenoise==6.7.0
